# AIOS Next Steps -- 2026-04-29b

Continuing from **v0.4.100**. Logging migration (Option 1) -- partial.

## Session Result: Boot/ Printf Migration to LOG_*

### Goal
Continue migrating ad-hoc `printf` calls in core boot path to the structured
`AIOS_LOG_*` macro infrastructure. Each LOG entry then automatically goes
to the ring buffer (/proc/log), serial console, and persistent log file
(/var/log/aios.log).

### Changes

**LOG_MODULE setup added** (3 files):
- `src/boot/boot_config.c`: `LOG_MODULE "boot"`
- `src/boot/boot_net_init.c`: `LOG_MODULE "net"`
- `src/boot/boot_dtb.c`: `LOG_MODULE "hw"` (header added, printfs not yet
  migrated -- they are HW info dumps, mostly informational)

**Printfs migrated** (19 total):

| File | Count | Notes |
| --- | --- | --- |
| `boot_fs_init.c` | 6 | Block/ext2 init errors -> LOG_ERROR/WARN, mount info -> LOG_INFO |
| `boot_services.c` | 7 | tty/auth spawn FAILED -> LOG_ERROR_V (with err code), passwd missing -> LOG_WARN |
| `boot_config.c` | 2 | hostname kept printf + LOG_INFO, network defaults -> LOG_WARN |
| `boot_net_init.c` | 1 | "Network initialized" -> LOG_INFO |

### Build status: Clean

Compiled successfully (build 1555). Disk image rebuilt with new kernel.

### Deferred for next session

- aios_root.c: 32 printfs (root task setup, banner, FATAL, UART IRQ binding)
- plat/qemu-virt/*.c: 50 printfs (hardware init -- mostly informational)
- pipe_server.c: 17 printfs (operational state)
- fs_server.c, net_server.c: 0 printfs but have silent error returns (LOG_ERROR audit)
- boot resilience: subsystem init failure -> recovery shell (not started)

## Modified Files

| File | Change |
| --- | --- |
| `include/aios/version.h` | 100 -> 101 |
| `src/boot/boot_fs_init.c` | 6 printfs -> LOG_*, + LOG_MODULE was already set |
| `src/boot/boot_services.c` | 7 printfs -> LOG_*, errors include err code |
| `src/boot/boot_config.c` | LOG_MODULE setup + 2 printfs -> LOG_* |
| `src/boot/boot_net_init.c` | LOG_MODULE setup + 1 printf -> LOG_INFO |
| `src/boot/boot_dtb.c` | LOG_MODULE setup (printfs unchanged) |

## Next Session Priorities

### Priority 1: Finish LOG_* migration
- aios_root.c (32 printfs): banner stays raw, post-log_init -> LOG_*
- plat/qemu-virt/*.c (50 printfs): blk_virtio errors are critical
- pipe_server.c (17 printfs): operational events

### Priority 2: Error path audit
- fs_server.c: every `return -1` should LOG_ERROR with reason
- net_server.c: same
- Audit dispatch tables for unhandled IPC labels (silent drop today)

### Priority 3: Boot resilience
- Wrap each subsystem init: log failure, continue if non-critical
- Recovery shell stub: if fs mount fails, drop to dash on /bin only
- Test: deliberately break /etc/passwd, verify boot still completes

### Priority 4: RPi4 stability (deferred from v0.4.98)
- mkflash_rpi4.sh single-script flasher
- Operator/recovery shell for hardware testing
