# AIOS Next Steps -- 2026-04-13b

Starting from **v0.4.90**, building to **v0.4.91+**.

## Completed in v0.4.91

### RPi4 HDMI Text Console -- DONE

- **VideoCore mailbox driver** (`display_vc.c`, 220 lines)
  - Ported fb_test.c mailbox protocol to seL4 context
  - Maps mailbox MMIO from DTB (`hw_info.vc_mbox_paddr`)
  - DMA page for tag buffer (seL4-allocated, known physical address)
  - Same tag sequence: set resolution 1024x768x32, allocate buffer, get pitch
  - Maps GPU-allocated framebuffer pages into root VSpace
  - Bus-to-ARM address conversion (`& 0x3FFFFFFF`)
  - Sets `gpu_fb/gpu_width/gpu_height/gpu_available` globals
  - All existing rendering code (gradient, boot text, splash) works unchanged

- **Framebuffer text console** (`fb_console.c`, 170 lines)
  - 128-column x 76-row character grid (8x10 pixel cells at 1024x768)
  - Uses `font8x8[]` from boot_display_init.c (95 ASCII glyphs)
  - Supports printable chars, newline, backspace, tab
  - Scroll via `memmove` of pixel data (one cell row up)
  - `fb_console_printf()` for formatted output
  - All functions no-op when display unavailable (safe on QEMU without ramfb)

- **Boot sequence integration**
  - `fb_console_init()` called at end of `boot_display_init()`
  - Explicit `fb_console_printf()` calls in `aios_root.c` at boot milestones:
    AIOS version, RAM/CPU, UART, DTB discoveries (eMMC/GENET/VC mbox),
    filesystem/network/display status, service startup
  - Boot messages visible on HDMI without serial adapter

- **SD card builder fixes** (`mksdcard.py`)
  - Fixed `get_elf_entry_point()`: reads entry address from ELF header (was broken)
  - `kernel_address` set to seL4 elfloader entry point (0x350f000)
  - Added `start_cd.elf` (copy of start4.elf) for old EEPROM compatibility
  - objcopy binary has .start at offset 0: no trampoline needed

- **USB HID keyboard design doc** (`docs/DESIGN_USB_HID.md`)
  - Full architecture: PCIe root complex + xHCI + USB enum + HID + keymap
  - RPi4 USB topology (VL805 behind PCIe)
  - Phased plan: U1-U4 across v0.4.93-96
  - Effort estimate: 4000-6000 lines, 11-17 weeks
  - DWC2 alternative analysis

### v0.4.91 Modified Files

| File | Changes |
| --- | --- |
| include/aios/version.h | Patch bump 90 -> 91 |
| projects/aios/CMakeLists.txt | Added fb_console.c to sources |
| src/boot/boot_display_init.c | Added fb_console.h include + fb_console_init() call |
| src/aios_root.c | Added fb_console.h include + boot status fb_console_printf() calls |
| scripts/mksdcard.py | Fixed ELF entry parsing, added start_cd.elf |

### v0.4.91 New Files

| File | Lines | Description |
| --- | --- | --- |
| src/plat/rpi4/display_vc.c | 220 | VideoCore mailbox framebuffer (seL4 driver) |
| src/boot/fb_console.c | 170 | Scrolling framebuffer text console |
| include/aios/fb_console.h | 17 | Console API header |
| docs/DESIGN_USB_HID.md | 170 | USB HID keyboard design doc |

### v0.4.91 Test Results

    QEMU boot (cortex-a53, no ramfb):    PASS (full boot to login)
    QEMU boot (cortex-a53, with ramfb):  PASS (fb_console renders boot messages)
    RPi4 build (306/306):                PASS
    RPi4 SD card image (193MB):          PASS (kernel_address=0x350f000)
    RPi4 hardware boot:                  PENDING (needs SD card flash + test)

## Completed in v0.4.90

### PAL Step 7 cleanup -- DONE

- Shared `plat_virtio_probe.c/.h`: single MMIO mapping + 32-slot scan, idempotent
- `blk_virtio.c` calls probe, removed MMIO map + scan (477->410 lines)
- `net_virtio.c` reads probe getters instead of extern bridge globals
- Bridge globals eliminated: `net_vio`, `net_vio_slot` removed from externs
- 5 dead globals removed from `aios_root.c`
- 3 dead files deleted: `blk_io.c` (178 lines), `boot_log_init.c` (96 lines), `net_driver.c` (92 lines)

### RPi4 Phase 1 -- Multi-target build + hardware verified -- DONE

- `settings-rpi4.cmake`: BCM2711, Cortex-A72, GICv2, 4GB, no hypervisor
- `AIOS_SETTINGS` cmake variable for multi-target builds
- Platform-conditional `PLAT_SOURCES` in CMakeLists.txt (`PLAT_QEMU_VIRT` / `PLAT_RPI4`)
- Stub drivers: `blk_emmc.c`, `net_genet.c`, `display_vc.c` (return -1, Phases 2-4)
- DTB parsers: `parse_emmc()`, `parse_genet()`, `parse_vc_mbox()` in boot_dtb.c
- `hw_info.h`: added `emmc_paddr/irq`, `genet_paddr/irq`, `vc_mbox_paddr` fields
- `scripts/mksdcard.py`: downloads RPi firmware, builds FAT32+ext2 SD card image
- `fb_test.c`: bare-metal framebuffer test via VideoCore mailbox
- Hardware verified on RPi4 Model B: HDMI output working (1024x768x32, gradient + text)
- QEMU regression: full boot + login + shell verified

### RPi4 boot chain learnings

- RPi4 EEPROM may request `start_cd.elf` instead of `start4.elf` (old EEPROM firmware)
- Fix: provide `start4.elf` renamed as `start_cd.elf` on FAT32 boot partition
- `gpu_mem=16` triggers cut-down firmware lookup (`start_cd.elf`); use `gpu_mem=64`
- seL4 elfloader for bcm2711 loads at `0x34f0000` (not default `0x80000`)
- `.start` section at file offset `0xf000`, entry point `0x34ff000`
- `.start` uses `adrp` (PC-relative) -- fully relocatable if relative layout preserved
- Bare-metal C: must use linker script + assembly entry stub to ensure `_start` is first
- VideoCore mailbox framebuffer: property channel 8, buffer address `& 0x3FFFFFFF` for ARM physical

### Build commands

```
# QEMU (default)
cd build-04 && ninja

# RPi4
mkdir build-rpi4 && cd build-rpi4
cmake -G Ninja -DCMAKE_TOOLCHAIN_FILE=../deps/kernel/gcc.cmake \
    -DCROSS_COMPILER_PREFIX=aarch64-linux-gnu- \
    -DAIOS_SETTINGS=settings-rpi4.cmake ..
ninja

# RPi4 SD card image
python3 scripts/mksdcard.py

# RPi4 bare-metal framebuffer test
aarch64-linux-gnu-gcc -c -ffreestanding -O2 -o /tmp/rpi4_fb.o src/plat/rpi4/fb_test.c
aarch64-linux-gnu-as -o /tmp/rpi4_entry.o /tmp/rpi4_entry.S
aarch64-linux-gnu-ld -T /tmp/rpi4test.ld -o /tmp/rpi4_fb.elf /tmp/rpi4_entry.o /tmp/rpi4_fb.o
aarch64-linux-gnu-objcopy -O binary /tmp/rpi4_fb.elf kernel8.img
```

## Next Priorities

### 1. RPi4 seL4 boot (requires serial adapter)

**Goal**: seL4 kernel + AIOS root task running on RPi4, serial output visible.

- USB-to-serial adapter: CP2102 or FTDI (~$5), GPIO 14 (TX) pin 8, GPIO 15 (RX) pin 10, GND pin 6, 115200 baud
- Boot the real seL4 image with trampoline:
  - `objcopy -O binary` the ELF to flat binary
  - Patch offset 0 with `b +0xf000` (ARM64: `0x14003C00`) to jump to `.start`
  - Set `kernel_address=0x34f0000` in config.txt
  - Or: use ELF directly (RPi firmware may parse ELF headers)
- Expected output: seL4 ELF-loader banner, kernel bootstrap, AIOS banner
- DTB will report: PL011 UART, eMMC, GENET, VC mailbox
- Block/net/display stubs will print "not yet implemented"
- Boot stops at getty spawn (no filesystem)

### 2. RPi4 Phase 2: SDHCI SD card driver

**Goal**: `ls /`, `cat /etc/passwd`, login prompt on RPi4 hardware.

- Implement `blk_emmc.c`:
  - BCM2835 SDHCI controller init from DTB (`hw_info.emmc_paddr`)
  - SD card init sequence: CMD0, CMD8, ACMD41, CMD2, CMD3, CMD7
  - PIO mode sector I/O: CMD17 (read), CMD24 (write)
  - IRQ binding for completion notification
- ext2 partition is partition 2 on the SD card
- `plat_blk_init()` returns 0, `plat_blk_read/write()` do sector I/O
- No log drive (single SD card -- `plat_blk_init_log()` returns -1)
- Reference: Linux `drivers/mmc/host/bcm2835-mmc.c`, Circle `lib/bcm2835-sdhci.cpp`
- Test: login on RPi4, `ls -la`, `cat /etc/passwd`, dash shell

### 3. RPi4 Phase 3: GENET Ethernet driver

**Goal**: SSH into RPi4, network commands.

- Implement `net_genet.c`:
  - BCM54213 GENET v5 MAC + MDIO PHY init from DTB (`hw_info.genet_paddr`)
  - DMA ring setup (256 TX + 256 RX descriptors)
  - RX poll into `net_rx_ring` (same SPSC interface as virtio-net)
  - TX via `plat_net_tx()` to descriptor ring
  - IRQ on RX completion -> `net_srv_ntfn_cap` signal
  - MAC address from DTB `local-mac-address` property
- Reference: Linux `drivers/net/ethernet/broadcom/genet/`, Circle `lib/bcmgenet.cpp`
- Test: `ping` from host, SSH session into RPi4

### 4. RPi4 Phase 4: VideoCore display integration

**Goal**: AIOS display server on HDMI, boot text, splash image.

- Implement `display_vc.c` (real driver, not bare-metal test):
  - VideoCore mailbox property interface (from fb_test.c patterns)
  - `plat_display_init()`: allocate framebuffer, return to display subsystem
  - `plat_display_get_fb()`: return mapped framebuffer pointer
- Wire into `boot_display_init.c` (gradient background, text rendering, splash)
- Test: AIOS splash screen on HDMI via display server

### 5. EEPROM update (optional)

- RPi4 EEPROM can be updated to fix `start_cd.elf` vs `start4.elf` naming
- Requires booting Raspberry Pi OS once to run `rpi-eeprom-update`
- Or: place `pieeprom.upd` + `recovery.bin` on FAT32 boot partition
- After update, EEPROM should request `start4.elf` directly

### 6. Other QEMU-side work (independent of RPi4)

- Dash improvements: tab completion, history, PS1 prompt
- TTY improvements: process-aware echo, virtual terminals
- ext2 triple indirect blocks
- File redirects across exec (server-side fd table)
- Allocator right-sizing (monitor with debug command)
- zsh Phase 2: interactive mode with ZLE

## Platform Status

| Platform | Build | Boot | Storage | Network | Display |
| --- | --- | --- | --- | --- | --- |
| QEMU virt (Cortex-A53) | PASS | PASS | virtio-blk | virtio-net | ramfb |
| QEMU virt (Cortex-A72) | PASS | PASS | virtio-blk | virtio-net | ramfb |
| RPi4 (BCM2711) | PASS | PASS (bare-metal) | stub | stub | VC mailbox (test) |

## File Inventory

### New in v0.4.90

| File | Lines | Description |
| --- | --- | --- |
| `src/plat/qemu-virt/plat_virtio_probe.h` | 32 | Probe interface |
| `src/plat/qemu-virt/plat_virtio_probe.c` | 85 | MMIO mapping + device scan |
| `src/plat/rpi4/blk_emmc.c` | 37 | SDHCI stub |
| `src/plat/rpi4/net_genet.c` | 23 | GENET stub |
| `src/plat/rpi4/display_vc.c` | 18 | VideoCore stub |
| `src/plat/rpi4/fb_test.c` | 190 | Bare-metal framebuffer test |
| `settings-rpi4.cmake` | 33 | RPi4 seL4 kernel config |
| `scripts/mksdcard.py` | 195 | SD card image builder |

### Deleted in v0.4.90

| File | Lines | Reason |
| --- | --- | --- |
| `src/boot/blk_io.c` | 178 | Absorbed into blk_virtio.c (v0.4.89) |
| `src/boot/boot_log_init.c` | 96 | Absorbed into blk_virtio.c (v0.4.89) |
| `src/servers/net_driver.c` | 92 | Absorbed into net_virtio.c (v0.4.89) |
