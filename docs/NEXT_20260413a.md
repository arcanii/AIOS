# AIOS Next Steps -- 2026-04-13a

Starting from **v0.4.88**, building to **v0.4.89**.

## Completed in v0.4.89

### Platform Abstraction Layer (PAL) -- Phase 0 complete

- **DESIGN_RPI.md**
  - Full design document for Raspberry Pi port
  - Multi-target build architecture (QEMU virt, RPi4, RPi5 future)
  - HAL interfaces: blk_hal.h, net_hal.h, display_hal.h
  - 5-phase implementation plan (PAL refactor -> serial boot -> SD card -> network -> display)
  - RPi5 analysis: no seL4 BSP exists, RP1 south bridge requires PCIe, RPi4 first strategy
  - HVF appendix: tested QEMU HVF acceleration on M3 Max, seL4 hangs (EL2 mismatch)

- **src/plat/ directory structure**
  - `src/plat/plat.h` -- platform dispatcher (modeled after src/arch/arch.h)
  - `src/plat/blk_hal.h` -- block device HAL (matches ext2 blk_read_fn/blk_write_fn)
  - `src/plat/net_hal.h` -- network device HAL (TX + driver thread + MAC)
  - `src/plat/display_hal.h` -- display device HAL (framebuffer init + get)

- **Block subsystem behind HAL (Steps 1-3)**
  - `src/plat/qemu-virt/blk_virtio.c` (476 lines) -- virtio-blk init + sector I/O
  - Contains `plat_blk_init`, `plat_blk_init_log`, `plat_blk_read/write`, `plat_blk_read_log/write_log`
  - Virtio MMIO mapping, device probe, DMA allocation, legacy handshake -- all moved from boot_fs_init.c
  - Log drive init absorbed from boot_log_init.c (removed from build)
  - `boot_fs_init.c` reduced from 260 to 55 lines (pure VFS/ext2 mount)
  - Privatized: `blk_vio`, `blk_dma`, `blk_dma_pa` (+ log variants) as static in blk_virtio.c

- **Network subsystem behind HAL (Steps 4-5)**
  - `src/plat/qemu-virt/net_virtio.c` (330 lines) -- virtio-net init + TX + RX driver
  - Contains `plat_net_init`, `plat_net_tx`, `plat_net_driver_fn`, `plat_net_get_mac`
  - Virtio-net DMA, queue setup, feature negotiation, IRQ binding -- all moved from boot_net_init.c
  - RX driver thread moved from net_driver.c (removed from build)
  - TX function extracted from net_stack.c (net_tx_send is now a thin stats wrapper)
  - `boot_net_init.c` reduced from 195 to 15 lines
  - `net_stack.c` and `net_tcp.c` no longer include virtio.h
  - Privatized: `net_dma`, `net_dma_pa`, `net_irq_handler_cap` as static in net_virtio.c
  - Bridge globals remain: `net_vio`, `net_vio_slot` (set by blk_virtio, read by net_virtio)

- **Display subsystem behind HAL (Step 6)**
  - `src/plat/qemu-virt/display_ramfb.c` (212 lines) -- fw_cfg + ramfb framebuffer
  - Contains `plat_display_init`, `plat_display_get_fb`
  - fw_cfg MMIO mapping, file enumeration, ramfb DMA config, FB page allocation
  - `boot_display_init.c` reduced from 439 to 246 lines (font + rendering + boot text)
  - No fw_cfg/ramfb references remain in generic code
  - Privatized: `gpu_vio`, `gpu_dma`, `gpu_dma_pa`, `gpu_vio_slot` removed from root_shared.h

- **Build system**
  - `PLAT_QEMU_VIRT` compile definition added
  - `src/` added to include path for `plat/` headers
  - Source list: `blk_io.c` -> `blk_virtio.c`, `net_driver.c` -> `net_virtio.c`, added `display_ramfb.c`
  - `boot_log_init.c` removed from build (absorbed into blk_virtio.c)
  - `boot_services.c`: `net_driver_fn` -> `plat_net_driver_fn`

- **Cortex-A72 compatibility verified**
  - QEMU `-cpu cortex-a72` (RPi4 CPU): full boot to login prompt, all 4 cores

## Modified Files

| File | Changes |
| --- | --- |
| include/aios/version.h | Patch bump 88 -> 89 |
| include/aios/root_shared.h | Removed 14 virtio globals, removed old blk/net declarations |
| projects/aios/CMakeLists.txt | PLAT_QEMU_VIRT, 3 platform sources, removed blk_io/net_driver/boot_log_init |
| src/boot/boot_fs_init.c | 260 -> 55 lines: calls plat_blk_init(), pure VFS mount |
| src/boot/boot_net_init.c | 195 -> 15 lines: calls plat_net_init() |
| src/boot/boot_display_init.c | 439 -> 246 lines: font + rendering, calls plat_display_init() |
| src/boot/boot_services.c | net_driver_fn -> plat_net_driver_fn, added net_hal.h |
| src/net/net_stack.c | net_tx_send wraps plat_net_tx(), removed virtio.h |
| src/net/net_tcp.c | Removed stale virtio.h include |
| src/plat/qemu-virt/blk_virtio.c | 484 -> 476 lines: removed gpu_vio recording |

## New Files

| File | Description |
| --- | --- |
| docs/DESIGN_RPI.md | Raspberry Pi port design document |
| src/plat/plat.h | Platform dispatcher header |
| src/plat/blk_hal.h | Block device HAL interface |
| src/plat/net_hal.h | Network device HAL interface |
| src/plat/display_hal.h | Display device HAL interface |
| src/plat/qemu-virt/blk_virtio.c | Virtio-blk init + sector I/O |
| src/plat/qemu-virt/net_virtio.c | Virtio-net init + TX + RX driver |
| src/plat/qemu-virt/display_ramfb.c | fw_cfg + ramfb framebuffer init |

## Test Results

    QEMU boot (cortex-a53, 4 cores):    PASS (full boot to login)
    QEMU boot (cortex-a72, 4 cores):    PASS (RPi4 CPU compatibility)
    ls -la after login:                  PASS (ext2 reads via plat_blk_read)
    No log drive:                        PASS (graceful fallback)
    No network device:                   PASS (net_available=0 path)
    No ramfb device:                     PASS (graceful fallback)

## Code Metrics

    Platform-agnostic boot files:
      boot_fs_init.c        55 lines  (was 260)
      boot_net_init.c       15 lines  (was 195)
      boot_display_init.c  246 lines  (was 439)

    QEMU platform drivers:
      blk_virtio.c         476 lines
      net_virtio.c         330 lines
      display_ramfb.c      212 lines

    Globals privatized:     14 (blk: 6, net: 3, display: 4, irq: 1)
    Bridge globals:          2 (net_vio, net_vio_slot -- Step 7 cleanup)

    git diff:  361 insertions, 691 deletions (-330 net)

## Completed in v0.4.90

### PAL Phase 0 cleanup (Step 7) -- DONE

- `plat_virtio_probe.c/.h`: shared MMIO mapping + 32-slot device scan (idempotent)
- `blk_virtio.c`: calls probe, removed MMIO map + scan + bridge globals (477->410 lines)
- `net_virtio.c`: reads probe getters instead of extern bridge globals
- Privatized: `net_vio`, `net_vio_slot` bridge globals eliminated
- Removed 5 dead globals from `aios_root.c` (`net_vio`, `net_dma`, `net_dma_pa`, `net_vio_slot`, `net_irq_handler_cap`)
- Removed 3 externs from `root_shared.h`
- Deleted 3 dead files: `blk_io.c` (178 lines), `boot_log_init.c` (96 lines), `net_driver.c` (92 lines)

### RPi4 Phase 1 -- Multi-target build + hardware verified -- DONE

- **Build system**: `AIOS_SETTINGS` cmake variable, `settings-rpi4.cmake` (BCM2711, Cortex-A72, GICv2)
- **Stub drivers**: `src/plat/rpi4/blk_emmc.c`, `net_genet.c`, `display_vc.c` (return -1, Phase 2-4)
- **Platform selection**: CMakeLists.txt conditional `PLAT_SOURCES` based on `AIOS_PLATFORM`
- **DTB parsers**: `parse_emmc()`, `parse_genet()`, `parse_vc_mbox()` in boot_dtb.c
- **hw_info.h**: added `emmc_paddr/irq`, `genet_paddr/irq`, `vc_mbox_paddr` fields
- **SD card builder**: `scripts/mksdcard.py` (downloads firmware, FAT32+ext2 image)
- **Hardware test**: bare-metal framebuffer test on RPi4 via VideoCore mailbox
- **RPi4 verified**: boots custom AArch64 code, HDMI framebuffer output working (1024x768x32)
- **QEMU regression**: full boot + login + shell verified

### v0.4.90 Modified Files

| File | Changes |
| --- | --- |
| include/aios/version.h | Patch bump 89 -> 90 |
| include/aios/root_shared.h | Removed 3 externs (net_vio, net_vio_slot, net_irq_handler_cap) |
| include/aios/hw_info.h | Added emmc, genet, vc_mbox fields for RPi4 |
| src/aios_root.c | Removed 5 dead globals |
| src/plat/qemu-virt/blk_virtio.c | Calls plat_virtio_probe, removed MMIO map + scan |
| src/plat/qemu-virt/net_virtio.c | Uses probe getters instead of extern bridge globals |
| src/boot/boot_dtb.c | Added parse_emmc, parse_genet, parse_vc_mbox + hw_report |
| projects/aios/CMakeLists.txt | Platform-conditional PLAT_SOURCES, AIOS_PLATFORM define |
| CMakeLists.txt | AIOS_SETTINGS variable for multi-target builds |
| settings.cmake | Added AIOS_PLATFORM variable |
| .gitignore | Added rpi4-firmware/ and .sdcard-tmp/ |

### v0.4.90 New Files

| File | Description |
| --- | --- |
| src/plat/qemu-virt/plat_virtio_probe.h | Shared virtio probe interface |
| src/plat/qemu-virt/plat_virtio_probe.c | MMIO mapping + 32-slot scan, idempotent |
| src/plat/rpi4/blk_emmc.c | BCM2835 SDHCI stub (Phase 2) |
| src/plat/rpi4/net_genet.c | BCM54213 GENET stub (Phase 3) |
| src/plat/rpi4/display_vc.c | VideoCore mailbox stub (Phase 4) |
| src/plat/rpi4/fb_test.c | Bare-metal framebuffer test for RPi4 |
| settings-rpi4.cmake | seL4 kernel config for RPi4 (BCM2711) |
| scripts/mksdcard.py | SD card image builder (FAT32 boot + ext2 system) |

### v0.4.90 Deleted Files

| File | Reason |
| --- | --- |
| src/boot/blk_io.c | Absorbed into blk_virtio.c in v0.4.89 |
| src/boot/boot_log_init.c | Absorbed into blk_virtio.c in v0.4.89 |
| src/servers/net_driver.c | Absorbed into net_virtio.c in v0.4.89 |

### v0.4.90 Test Results

    QEMU boot (cortex-a53, 4 cores):        PASS (full boot to login)
    QEMU net probe (net=yes):               PASS (virtio-net via probe)
    ls -la after login:                     PASS (ext2 via plat_blk_read)
    Log drive mount:                        PASS
    RPi4 build (340/340):                   PASS
    RPi4 SD card image (193MB):             PASS
    RPi4 hardware boot:                     PASS (kernel executes)
    RPi4 HDMI framebuffer (1024x768):       PASS (VideoCore mailbox)

### v0.4.90 Code Metrics

    Shared probe:
      plat_virtio_probe.c       85 lines (new)
      plat_virtio_probe.h       32 lines (new)

    QEMU drivers (updated):
      blk_virtio.c             410 lines (was 476)
      net_virtio.c             335 lines (was 331)

    RPi4 stubs:
      blk_emmc.c                37 lines
      net_genet.c               23 lines
      display_vc.c              18 lines
      fb_test.c                190 lines (bare-metal test)

    Globals privatized:     5 removed from aios_root.c
    Dead files deleted:     3 (366 lines removed)
    git diff:  148 insertions, 469 deletions (-321 net)

## Next Priorities

### RPi4 Phase 2: SD card driver (ext2 from disk)

- Implement BCM2835 SDHCI PIO mode (CMD17/CMD24) in blk_emmc.c
- DTB-driven MMIO address and IRQ
- Boot to login prompt on RPi4 hardware
- Requires serial adapter for debugging

### RPi4 Phase 3: Network

- BCM54213 GENET Ethernet driver in net_genet.c
- MDIO PHY init, DMA ring I/O
- SSH into RPi4

### RPi4 Phase 4: Display integration

- VideoCore mailbox driver in display_vc.c (real driver, not bare-metal test)
- Wire into AIOS display server for boot text and framebuffer
