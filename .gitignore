# Build artifacts
build/
build-*/
*.img

# Model weights (too large for git)
*.bin

# Training outputs
out/
__pycache__/
*.pyc

# OS files
.DS_Store
include/aios/build_number.h
include/aios/build_time.h
*.bak
*.bak[0-9]
aios_system_roundtrip.xml
*.elf
*.o
*.old
programs/*.BIN

# Stale root-level artifacts from earlier phases
fs_data
fs_server
offset
readme_report
libc/
code*.bin

# Temp scripts
/tmp/
*.stamp

# Editor swap files
*.swp
*.swo
.vscode/
.idea/

# Serial console captures (hw/rpi4 sessions)
screenlog.*

# Disk images (built, not tracked)
disk/*.img
disk/rpi4-firmware/
disk/rpi4-firmware-latest/
disk/.sdcard-tmp/

# Dependencies (cloned, not tracked)
deps/*
# ...except the pinned-dependency patch set (see DEPS.md + build_environment.sh)
!deps/patches/

# Keep dead code archive for history
!ref/dead_code/
!ref/dead_code/**
src/lib/aios_posix.c.pre_modular

# aios_console.py live mirror (watch viewer)
.aios_console.live

# clangd database (symlink into build-04/)
compile_commands.json
.cache/

# AI tooling: Claude config/skills + the installed BMad modules (developer-local, not OS source)
.claude/
_bmad/
